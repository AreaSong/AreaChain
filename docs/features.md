# 功能清单

对照当前代码，AreaChain 已实现的能力如下。刻意不做的范围见文末边界声明。

## 1. 壳与工作台架构

| 能力 | 说明 |
|---|---|
| 菜单栏浮层 | 基于 `NSStatusItem` + `NSPopover`，挂在顶部菜单栏，平时无 Dock 图标（`LSUIElement` + `.accessory`）。打开工作台或手记小窗会升为 `.regular`，Dock 会出现；关闭最后一扇后回到 accessory。浮层页签是「**任务 / 手记**」，底栏「工作台」图标按钮直接打开对应页面（悬停提供 Tooltip 说明）；「⋯」提供语法帮助、设置和退出。左侧筛选、中间语法搜索（占位符为「搜索待办与语法...」并享有更大横向呼吸宽度），筛选支持点击、悬停和 ⌘⇧F，在底栏上方打开两级浮层；任务页可选日期、优先级和标签，手记页只有标签，底栏本身保留。已生效条件显示在搜索框里，可单独移除。顶部状态指示器与副标题解耦：任务页按待办状态显示橙点/绿勾/灰勾与「今天还剩 N 项/今日待办已完成/今日无待办」；手记页按手记条数显示印章圆点与「今日手记 N 篇」，空状态显示羽毛笔与温馨引导（彻底消除手记误现孤立橙点）。底栏不再重复显示完成百分比。macOS **⌘,** 仍打开同一套 `SettingsView`。浮层「任务」页与工作台同一套昨天/即将芯片；筛选各自使用浮层和今日过滤条，不共用同一条控件；展开昨天遗留提供未完成计数与「全部延至今日」快捷操作；任务行复选框紧跟标题（保障绝对统一的垂直左对齐线），四象限优先级点移至标题尾部；今日任务全部完成时以紧凑轻量的通透完成标呈现。浮层「手记」页采用严格固定 34pt 单行捕获框（与任务框像素级对齐，消除 Tab 切换跳动与长文本输入时下方列表抖动），长文本横向平滑滚动，包含私密标签时图标动态变为盾牌 🛡，回车或 ⌘Return（或点击「⌘↩」按钮）保存；右侧独立小窗按钮全时段可用（空草稿点击即可新建空白手记小窗）。手记列表支持单击整行高亮选中、双击整行呼出独立大窗口，悬停/选中淡入操作按钮，并打通完整键盘流（↑/↓ 切换选中、Return/⌘O 打开、⌘C 复制、Delete 移入废纸篓、Esc 取消选中）。完整内容可在独立小窗阅读、续写及置顶。 |
| 菜单栏单一状态图标 | 入口固定 24pt 宽，18×18pt 模板图像由系统着色。书本内的小点表示仍有事项，内嵌勾号表示今日已处理完，暂无安排时为普通书本；没有独立数字区，也不显示 `0` 或 `99+`。悬停与无障碍标签提供准确数量，包含今日未完成待办与未打卡习惯，和侧栏口径一致。初次读取前显示普通图标并提示状态暂不可用，读取失败保留上次状态。数量、状态或语言变化都不会改变占位。 |
| 三栏大屏工作台 | `MainSplitWorkspaceView`：左侧 `WorkspaceSidebarView` 按「总览 / 事项 / 规划 / 内容 / 组织 / 系统」分组。总览是 Dashboard；事项是今日、待处理、全部事项；规划是日历、安排、四象限；内容是灵感手记、附件；组织是平面标签入口和各标签；系统是设置、隐私与解锁、数据与备份、回收站。搜索留在顶部搜索框，不进侧栏。没有项目树。标签管理页已接通，侧栏会列出各个标签。今日、待处理和全部事项已实现：待处理页内切换逾期与即将，不另开侧栏入口；全部事项同时包含一次性事项和重复事项，子任务仍属于父事项。重复事项没有独立侧栏入口，待处理状态来自真实排定日投影。行上同时保留星期规则和下一次排定日。Dashboard 已接通：今日、近 7 日和热力图共用同一套日统计，只计排定日上的实际完成；跳过、非排定日和停用后的标记不计入完成，也不增加热力图强度。工作台进度环用 `todayOneOffProgress`（不含重复事项）；侧栏今日角标和菜单栏「今天还剩」用 `DayBoardLogic.todayProgress`（跳过视为已闭合）。子任务不另计；逾期和即将复用 `AgendaProjection.pending`。热力图按完成数分 0 到 4 档；点真实日期打开日历该日。活动只投影已有日期字段，最近 30 天最多 12 条，私密手记不生成活动。这不是持久化审计日志。设置、隐私与解锁、数据与备份已拆成三个页面。搜索筛选交集、菜单栏与今日/手记筛选共享已接到现有规则；浅深色和最小窗口已有 WorkspaceRenderingTests 覆盖；2026-09-26 的隔离 QA 全量测试已通过，证据见工程手册。右栏 `TaskDetailDrawer`。 |
| 任务检查器抽屉 | `TaskDetailDrawer`：行内改标题、纯文本多行备注、一层子任务、2×2 四象限选择器（宫格文案是「重要且紧急 / 重要 / 紧急 / 其余」）、提醒快捷点（09:00 / 12:00 / 15:00 / 18:00 / 20:00，再点可取消）。**仅待办**有排期芯片（今天 / 明天 / 后天 /「选一天…」）；习惯没有改期芯片，用「重复星期」。另有多个标签、图文附件、创建时间与来源 App。习惯抽屉另含连击卡（当前 + 历史最高 + **检查日**状态：已打卡 / 已跳过 / 非计划打卡日 / 习惯已暂停 / 待打卡）；星期掩码不允许一天都不选；标题栏可按**当前检查日**勾选（日历选中日、昨天芯片、方向键换条后的那一天）。**标签专属清单**打开习惯时把检查日接到今天；切到「今日」也会把 leftover 日历日复位到今天。重复事项不再作为侧栏入口。工具栏可显示/隐藏抽屉；备注正在编辑时 `Esc` 先退出输入；**抽屉**标题 `Esc` 回滚草稿，点别处会保存。清单行内改名是回车或勾保存，`Esc` 或叉取消，点别处不会保存。工作台里否则 `Esc` 先关检查器（保留选中），再清列表焦点或多选。浮层没有检查器，`Esc` 只失焦输入或取消列表焦点。关检查器后再点同一条会重新打开。方向键在检查器关闭时只换焦点、不打开检查器；**检查器已打开时会把抽屉换到当前行**，并更新检查日。 |
| 今日进度环 | 工作台「任务」页（页内标题「今日事项」）用 `DaybookProgressRing` 显示临时待办完成比例，**不含习惯**。 |
| 窗口路由 | 工作台仍由 `AppWindows.openWorkspace(tab:)` 在同一窗口切 tab。单条手记由 `DiaryWindows` 打开独立小窗，同一记录只开一扇；可调整大小并选择窗口置顶，收起浮层不关闭。窗口置顶与列表置顶独立，关设置/工作台不隐藏手记小窗。日历、四象限、安排、附件及回收站保持原路由。 |
| 打开即聚焦与草稿记忆 | 任务捕获和浮层手记草稿由同一个 `BoardComposerSession` 持有，类型都是 `BoardComposerDraft`，均在本次运行保留，不落盘。任务只用正文；手记另外保留标签选择，锁定时只封存手记草稿。手记可把草稿转入小窗续写，不预先保存或复制记录。独立小窗显式保存，关闭及应用退出前确认未保存内容；失败保留，外部编辑冲突不静默覆盖。工作台今日页未提交的捕获草稿记在 `WorkspaceNavigation` 上，顶部搜索替换内容区时不清除，回车保存成功后才清空。待处理和全部事项的页内筛选同样保留；离开这两个页面后再进入时，待处理按逾期/即将规则重新打开，全部事项筛选回到默认。 |
| 打卡微动效与沉底体验 | 待办与习惯勾选时提供细腻物理级交互：`ModernCheckbox` 基于贝塞尔路径实时生长绘制对勾（Path Trim 动画），配合微弹性缩放与触控板触感反馈（`DaybookHaptics`）；任务标题 `ModernTaskTitle` 从左至右平滑滑出删除线并柔和渐淡。勾选后进入 **0.4 秒驻留防反悔期**，期间再次点击可秒级原地撤销；0.4 秒后以 `DaybookMotion.collapse` 弹簧平滑折叠沉底收纳进「已完成」折叠区域。顶部 Header「今天还剩 N 项」与习惯连击徽章「🔥 N」支持数字滚轮滚动过渡（`.numericText`），任务全清时状态点平滑变形为勾号。系统「减弱动态效果」（Reduce Motion）开启时自动降级为即时无延迟响应；自动化测试环境下自动直调，保证测试确定性。 |
| 清单键盘导航 | `DayBoardList`：`↑` / `↓` 在未完成待办 → 习惯 →（已展开时）已完成之间移动（仅绑定了焦点的清单；日历页不吞方向键）。待处理和全部事项在按钮、菜单或文本输入聚焦时不接管方向键和空格；没有选中行时不吞掉空格、回车、删除和 Esc。检查器关闭时方向键不打开它；**已打开时抽屉会跟到当前行**。捕获框为空再按 `↓` 退出输入并选中第一项（不打开检查器）；`Space` 勾选进入统一的 0.4s 动效与防误触沉底队列（检查日跟点选：昨天芯片勾**昨天**，点回今日行勾**今天**），并在沉底后平滑迁移焦点至下一项；`Return` 展开检查器。浮层 Return 会打开工作台「任务」页（退出标签过滤）；工作台里已有检查器回调时不再重开窗口。`Delete` 对待办或习惯弹出「移到回收站？」确认；`E` 进入标题就地编辑（Caps Lock 不影响）；备注或抽屉标题正在编辑时 `Esc` 先退出输入（抽屉标题草稿回滚，点别处会保存）；清单行内改名点别处不保存。工作台否则先关检查器，再取消列表焦点回到输入框。监视器只处理本窗口按键，浮层与工作台不会对同一次 Space 各 toggle 一次。工作台 composer 的 ⌘Return 不会记手记，也不会当成回车新建待办；Shift+Enter 支持换行备注。 |
| 跨日零点自动刷新 | 全局 `DayClock`，零点更新「今天」基准、重算角标、重排当天通知。 |

## 2. 捕获与自然语言解析

| 能力 | 说明 |
|---|---|
| 极速捕获输入框 | **浮层「任务」页**：回车记为今天的临时待办，不显示「**添加**」按钮；点「**手记**」或 **⌘Return** 写入灵感手记（解析 `#标签` 并自动打分类），并**切换到浮层手记页**。输入框右侧是「⌘↩」手记按钮，输入时在框下方显示实时解析预览。浮层 `Shift+Enter` 插入换行（多行备注）。**工作台「任务」页** composer 只有回车添加待办，没有「手记」按钮或 ⌘Return。日历页、标签专属清单和重复事项的新增使用同一套语法；专属清单新待办记到**今天**并带上该标签。 |
| 自然语言智能解析 (`NaturalLanguageParser`) | 待办与习惯的新增和标题编辑统一使用，包含日历、标签页、重复事项管理及抽屉。提取并洗净标题：<br>1. **时间**：`15:30`、`09:30`、`@15:30`、`下午3点半`、`晚上8点`、`上午9点15分`、`中午12点`、`中午12点和…`、`下午3点开会` 等，写入 `remindMinutes`；「修复3点问题」「下午3点问题」这类「点」后直接跟「问题」不解析为时刻；无「上午/下午…」前缀时，「点」后还需空白、标点、`#@!` 或「和/跟/与/在/去/到/给/把/从/向」（如「3点去开会」会抽出 03:00）；<br>2. **优先级**：`!p1`~`!p4`（大小写均可）、`!重要且紧急`、`!重要紧急`、`!紧急重要`、`!重要不紧急`、`!不重要紧急`、`!重要`、`!紧急`；预览胶囊与四象限页同一套文案（`!p1`→「重要且紧急」，`!p2`→「重要」，`!p3`→「紧急」，`!p4`→「其余」）；<br>3. **标签**：收集**各行全部可挂到待办的** `#标签名`，保存时复用或创建；支持多个标签及带空格的 `#"标签名称"`；「密码 / 小巧思 / 日记」不会挂到待办上，`#密码` 也留在标题里（预览胶囊不出现），后面若还有普通标签则挂那个。手记仍会解析 `#密码` 并打分类。侧栏或抽屉新建/重命名待办标签若用这三个名字，会提示并保持表单打开。<br>4. **多行**：首行标题，其余进 `notes`。<br>输入时用胶囊即时预览时间 / 标签 / 优先级。**不解析**「明天 / 后天」等日期词，`dayKey` 由新增入口决定（今日或日历选中日），「明天」会留在标题里。 |
| 底栏语法搜索 | ⌘F 聚焦，跨日期查找待办（含已完成）、子任务、启用习惯和手记。支持关键词、多个 `#标签`、`!p1`～`!p4`、`@15:30` 组合；候选向上展开，Tab/Return 补全；只查询，不创建或修改标签。当前页签的筛选与查询取交集，日期范围和提醒是否设置会真正参与匹配，不改写用户输入；结果在浮层内显示。任务和子任务点击进入工作台日历并打开检查器，日期用命中的 `dayKey`；手记点击打开独立小窗。Esc 依次关闭候选、清空查询、退出输入，搜索中不触发捕获框的 ⌘Return。菜单栏里改的任务/手记筛选与工作台今日、手记页是同一份内存状态。 |
| 全局快捷呼出 | 默认 **⌘⇧A** 唤起或收起浮层，可在设置中重录。热键必须含 ⌘ / ⌥ / ⌃ 之一，纯 Shift 无效；粘贴热键不能与唤起热键相同（相同时粘贴热键停用，设置里会标明）。 |
| 剪贴板一键加今天 | 默认 **⌘⇧V** 单次读取剪贴板：有文字则**整段当标题**（保留原文并识别标签，不解析时间或优先级），同时有图也不挂图；仅图片则建一条待办并挂图片附件；为空不触发。不监听系统剪贴板。 |
| 前台应用戳记 (可选) | 设置「捕获 → 记下当时的 App」开启后，浮层回车、工作台「任务」页输入、剪贴板热键、日历页与标签清单添加待办时记录前台 App 的 Bundle ID；前台是 AreaChain 自己则不写。不读窗口标题。 |

## 3. 重复事项与打卡连击

用户看到的是统一「事项」。重复事项是事项属性，底层仍为 `DailyRoutine` 与 `RoutineCheck`。周期在检查器中改；今日列表打卡与跳过。新建重复事项在今日页，不在设置里，也不新增侧栏入口。管理全部重复事项（含停用）从今日页的重复图标面板进入，复用 `ResidentsPage`。

| 能力 | 说明 |
|---|---|
| 连续打卡与连胜算法 (`HabitStreakLogic`) | 追踪 `currentStreak` 与 `bestStreak`：<br>1. 行上与抽屉火焰徽章 `🔥 N`；抽屉另显示历史最高与**检查日**状态（已打卡 / 已跳过 / 非计划打卡日 / 习惯已暂停 / 待打卡）；<br>2. 非排定日透明桥接，不中断连胜；<br>3. 「今天跳过」视为合理闭合，保留连胜；<br>4. 当天尚未打卡不破击，漏掉**历史排定日**才清零；<br>5. 非排定日若仍打卡，连击 **+1**。 |
| 周期掩码重复 (`WeekdayMask`) | 任意勾选周一至周日；未勾选日不出现在今日列表、不计入角标、不催促通知。停用记下 `pausedOnDayKey`，暂停期间不当漏打，连击保留；再打开时把暂停那几天记成跳过（不含今天）。升级前就停用、没有暂停日的习惯，启用时从最后一次打卡（否则创建日）补跳过。 |
| 勾完与取消打卡 | 勾选记录 `RoutineCheck`；再点取消完成，并清掉跳过标记。 |
| 今天跳过 | 记 `isDone = true && isSkipped = true`，已完成区显示「已跳过」，明日恢复出现。 |
| 到点本机通知 | 可配提醒时刻；未配置则静音。 |
| 标签 / 四象限分类 | 可打多个平面标签、分配重要/紧急，列表显示分类小点。待办侧标签不含手记三分类。没有项目。 |
| 重复事项面板 (`ResidentsPage`) | 从今日页打开，不是侧栏页。页头「关闭」回到今日。列表、排序、启停和检查器仍走原逻辑。 |
| 详细长文本备注 (`notes`) | 多行纯文本；抽屉可编辑。备注中的 URL 可点开系统浏览器。 |
| 首次启动预置 | 空库首次打开预置「写日报」（工作日）与「复盘」（每日）；英文界面为 Daily log / Review。 |

## 4. 临时待办、子任务与备注

内容输入中的 `#标签` 在提交时创建或复用并关联，支持多个标签、跨行及引号名称。手记新增和编辑都识别标签，正文原样保留；子任务有独立标签，在标签页单独展示和计数，不挂到父任务。搜索不创建标签，标签名保持纯文本。代码、链接、`\#` 保留字面含义。删除文字不自动解绑。

临时待办底层为 `TodoItem`。

| 能力 | 说明 |
|---|---|
| 增删改与就地编辑 | 圆圈只完成/取消当前任务；中间标题与空白单击立即选中、双击就地改名；右侧 ⋯ 只打开菜单。日清单与标签清单支持 Shift 可见范围选择、⌘ 增减选择，普通点击恢复单选；不跨昨天/即将预览分区。删除会弹出「移到回收站？」确认，时间胶囊仍可单击改提醒。 |
| 灵活动态排期 (`dayKey`) | 可移到今天、明天、后天，或点「选一天…」打开日期选择。NLP **不会**因「明天」改 `dayKey`。 |
| 子任务系统 (`SubtaskItem`) | **仅一层**，不是无限多级：<br>1. 每个待办可含若干子任务（排序、标题、完成状态）；<br>2. 行上 `2/3` 胶囊，点击展开内联勾选；<br>3. 抽屉支持回车连续新增、行内改名与单项删除；<br>4. 勾完父任务（含昨天芯片补勾）时级联完成未完成子任务；父任务进回收站时，当时未删的子任务与附件打上同一软删时间戳；恢复父任务只还原这批，先前单独删掉的不会被救回。<br>5. JSON 快照含全部子任务（含已软删），便于回收站里的父任务往返备份。 |
| 详细长文本备注 (`notes`) | 原生多行文本编辑器，支持标签及显式优先级、提醒语法，不是富文本。行上截取备注第一行作预览。点击保存、⌘Return、失焦或离开页面时提交；输入中保留草稿，不为未完成的标签提前写库；失败保留本次运行的备注草稿并显示「未保存」，可回到原项重试。 |
| 底部浮动批量操作 | 批量栏出现在**标签专属清单、待处理和全部事项**。「今日」页支持范围高亮，但不提供批量操作栏。只选一次性事项时可改期、完成、加减标签和移到回收站。只选重复事项时可加减标签、启用或停用、移到回收站；标签清单里今天排定的可以打今天的卡，待处理逾期通道打最近一个未闭合排定日，不会改成今天。不能把整条重复事项标成永久完成。混合选择只保留两边都支持的动作。保存失败保留选择和筛选。专属清单列出待办、该标签下全部未删除的重复事项（非今日排定和停用也列出，当天已闭合的进已完成区），以及子任务。 |
| 列表智能过滤条 | 工作台「今日」过滤条始终出现，可按标签、来源 App、优先级和提醒筛选；即使没有昨天/即将芯片、筛选尚未生效也挂着。浮层用底栏上方的浮层选择：任务页是日期、优先级和标签，手记页只有标签。两边共用本次运行的筛选状态，但不是同一条控件。从工作台带入的提醒和来源会显示成可移除胶囊，浮层里不能新选这两项。条件作用于今天、昨天与即将列表，也可以清空。手记三分类不进入待办筛选。已完成默认折叠。 |
| 多图附件与截屏 | 待办与习惯行菜单：「附上图片 / 从剪贴板贴图 / 截当前屏」（指针所在显示器，找不到再退回主屏）。待办与习惯抽屉内可选图与粘贴；手记卡片目前只能选图。父项进回收站时，当时还活着的附件一并软删，「附件」页不再显示。父项已删、不在库里、或仍在回收站时，都不能单独恢复附件。行内缩略图，点击看原图。 |
| 昨天未完成与即将待办 | 浮层「任务」页与工作台「任务」页同一套顶部**芯片**：文案是「昨天 / 即将」加数字（默认折叠，点开才展开）。展开后区块标题是「昨天未完成 / 即将」，展示未完成数量，并提供「全部延至今日」快捷按钮一键顺延昨日待办。昨天区含未完成待办与**未打卡习惯**，可补勾；待办可改期到今天，习惯不能改期。点行会打开检查器并记下检查日。同一习惯若昨天未打、今天也该打，点昨天芯片再空格勾**昨天**，点今日行再空格勾**今天**。方向键在今日清单内移动时检查日回到今天。「即将」为未来排期待办。 |

## 5. 多维看板与视图

以下视图均在三栏工作台侧栏切换，不是独立窗口。

| 视图 | 说明 |
|---|---|
| 日历月视图 (`CalendarPage`) | 月网格显示每日未完成数字（口径同角标：未完成待办 + 未打卡习惯）；点日期看该日一次性事项和重复事项。工作台宽布局和窄布局都用同一套上月、下月、回到今天，不因嵌在工作台里拿掉月份切换。选中日可「加到这一天」（支持语法，保留选中日期）。点清单项打开检查器。仅日历页列表 `allowsTodoDrag: true`，可将**临时待办**拖到另一天格子改期（习惯不可拖）。方向键不用于清单导航，以免抢走月网格。 |
| 四象限矩阵 (`QuadrantPage`) | 宫格标题是「重要且紧急 / 重要 / 紧急 / 其余」（副文案分别为立即处理 / 制定规划 / 授权委托 / 顺延简化）；只列出该日**未完成**项，象限间拖拽改优先级。点卡片打开检查器。可按日前后翻，不只「今天」。 |
| 安排甘特图 (`GanttPage`) | 侧栏名「安排」。当月时间线：只显示**未完成**临时任务的**单日**色块，⌘ 点任务名或色块增减多选，Shift 按可见顺序连选。拖动已选色块或按 ←/→，全部选中任务按同一天数平移，保留日期间隔，并整体限制在当前月内（不是拉伸工期；模型只有一个 `dayKey`）。拖动只显示一份吸附预览，松手后一次保存；Esc、移出时间轴或窗口失焦取消，保存失败整组回滚。多选仅用于安排页改期，不显示标签清单的批量操作栏。点行名、双击色块或按 Return 打开检查器。重复事项在当月适用星期打点（不是完成记录，不参与多选改期）；点习惯名时，若该习惯**今天也有点**则检查今天，否则检查当月第一个打点日。 |
| 灵感手记 (`DiaryPage`) | 长期记录小巧思、密码备忘和日记，支持多个标签、正文 `#标签`、关键字自动分类、置顶、双击编辑、选图和软删。工作台可搜索及筛选；浮层使用固定单行 34pt 快速捕获框（支持长文横向滚动与多行粘贴防撑高，右侧独立小窗入口空草稿直接新建）与共享底栏。列表条目支持单击选中高亮、双击打开独立窗口，操作按钮悬停/选中淡入，支持键盘上下导航、回车/⌘O打开、⌘C复制、Delete移入废纸篓与Esc取消选中。启用「隐私与解锁」后，任一私密标签命中即加密整条正文和图片，捕获框动态显示盾牌反馈；移除或删除标签不自动解除保护。锁定不搜索私密正文，查看、复制、编辑与附件入口统一验证；解锁不自动展开其他记录。卡片和快速草稿失焦遮罩，锁定时加密封存；卡片被搜索过滤移除时仍保留未保存会话。未启用加密的旧密码分类仍只是防窥遮罩。 |
| 单条手记小窗 | 正文或行尾箭头主动打开；输入区可把草稿转入小窗。默认不置顶，窗口层级开关与记录排序独立；支持 ⌘S / ⌘Return 显式保存。保存失败保留草稿，关闭和退出可保存、不保存或取消；外部编辑冲突不覆盖，删除后不静默重建。私密内容默认遮罩，离开窗口/应用或保存后重新遮罩；附件仍执行相同隐私检查。 |
| 附件中心 (`AttachmentBrowserPage`) | 侧栏名「附件」。按「一次性事项 / 手记 / 重复事项」聚类缩略图，右键「进回收站」，原图预览。页内提示含习惯。父项已删或不在库里的附件不出现。待办行与待办/习惯抽屉均可选图或粘贴。 |
| 工作台顶部搜索 | 跨日期搜索待办标题／备注、习惯名／备注、子任务标题及手记。支持多个 `#标签`，待办和习惯还支持 `!p1`～`!p4`、`@15:30`；停用习惯不出现。逾期筛选使用与待处理相同的真实逾期检查日，其他日期范围仍用从今天起的下一个排定日。已加密私密正文锁定时不参与匹配，解锁后只在内存中检索；私密结果和帮助提示不显示正文。工作台搜索留在顶部，与关键词取交集，就地打开检查器，避免清掉查询。没有独立搜索页或侧栏入口。菜单栏点任务进入日历及检查器，点手记打开独立小窗。 |
| 回收站 (`TrashPage`) | 软删除的任务、习惯、手记、附件与标签（种类文案是「一次性事项 / 重复事项 / 手记 / 附件 / 标签」）；单项「恢复」或「彻底删除」，顶栏「清空」。父项已删或不在库里的附件不能恢复（按钮说明「请先恢复所属待办、习惯或手记」），只能彻底删除。 |
| 设置 (`SettingsView`) | 只管理语言、外观、登录时启动、全局快捷键、捕获、通知和系统日历。iCloud 一节只说明本版本不同步，没有可拨动开关。不含 JSON、加密备份、隐私库或数据库重置。 |

## 6. 通知系统

| 能力 | 说明 |
|---|---|
| 本机横幅提醒 | `UNUserNotificationCenter`，无额外后台常驻进程。 |
| 权限动态申请 | 浮层或工作台「任务」页输入写出时刻、以及检查器/行菜单/重复事项管理里改提醒时申请；设置「通知」里可点「允许通知」。 |
| 精准智能排程 | 习惯按生效星期排下一次提醒，排除已提前完成/跳过的日期；待办按指定日排程。时间按当地墙上时钟计算，夏令时不存在的时刻顺延至首个可用时刻，重复时刻取第一次。完成或跳过后重新排程。 |

## 7. 数据持久化与系统集成

| 能力 | 说明 |
|---|---|
| SwiftData 7 张核心表 | `DailyRoutine`, `RoutineCheck`, `TodoItem`, `SubtaskItem`, `DiaryEntry`, `TagItem`, `AttachmentItem`。没有项目表。标签是平面多标签，可挂在一次性事项、重复事项、子任务和手记上。手记新增记录级保护、密文与密钥标识；标签新增私密规则和颜色标识；附件保存密文存储指针和待清理原文件指针。旧 JSON 里的 `projects` / `projectID` 会被忽略，不会转成标签。旧开发库若因删掉项目实体打不开，需要按受控流程重建，不会静默删磁盘文件。 |
| 容灾与安全机制 | 库打不开时降级内存库并提示，不静默删除原库。 |
| 快照导入与导出 | 普通 JSON 保留普通记录、子任务、软删状态及附件元数据，不含图片二进制；排除受保护及旧密码遮罩手记和其附件。导入预览与提交前校验重复 UUID、拥有者、打卡业务键等冲突，且禁止覆盖已有私密记录／附件或绕过私密标签。错误时撤销本次导入并保留调用前编辑，不自动清洗数据。 |
| 私密锁与加密备份 | 系统验证与独立主密码可单独或同时启用，任意一种成功即可；至少保留一种可用方式，凭据撤销失败可追踪重试。AES-GCM 保护正文和附件，主密码派生包装数据密钥；首次转换须先保存、回读验证独立口令的隐私迁移备份。日常加密备份在「数据与备份」，含全部记录及图片，恢复时使用目标私密锁重新加密并合并，不默认全库覆盖。旧明文清理完成前明确提示，不自动删除外部备份或原始图片。 |
| 系统日历同步 (可选) | 只同步专属「AreaChain」日历中的临时待办。使用本机同步基线比较双方变化：单边修改才传播，双方冲突或旧绑定缺基线且不同则保留并提示，核对一致后可重试。坏库降级、读失败、基线损坏时禁止写入/清理。按年分片查询并补查绑定 ID，保留全天 GMT 民事日期规则。远端删除只解绑，普通编辑不会重建事件，完成后重新打开可重新发布。仅删除已确认绑定、远端未改的已完成/软删待办事件；未知遗留事件保留。跨系统部分失败明确报错，不假报同步成功。 |
| 登录自启 | `SMAppService.mainApp`，设置「启动 → 登录时打开」。 |

## 8. 明确不做 (边界声明)

1. **不做外部会议收割**：不导入其它日历的会议或活动。
2. **不做番茄钟倒计时**。
3. **不做后台剪贴板监听**：热键按下时单次读取。
4. **不做甘特前置依赖与工期拉伸**：只有单日色块拖放到另一天改期。
5. **不做自建云端账户**：数据在本机 SwiftData。设置里的 iCloud 只说明本版本不同步，不再保存无效偏好。
6. **不做 NLP 日期词解析**：输入「明天」不会改 `dayKey`。
7. **不做成套独立功能窗口**：日历、四象限、安排、附件和回收站在工作台内切 tab。单条手记仍由独立小窗打开，同一记录只开一扇。

## 9. 仍按产品边界保留的行为

这些不是未修的 bug，而是当前刻意保持的范围：

- 「今日」页支持范围选择，但不提供批量操作栏。批量栏只在标签专属清单、待处理和全部事项；圆圈始终只操作当前任务。
- 工作台「任务」页输入框没有 ⌘Return 记手记，手记走浮层「手记」页右侧「⌘↩」或侧栏「灵感手记」。
- 普通 JSON 不含图片二进制；独立口令的加密备份包含图片，二者不可互相替代。
- 「密码 / 小巧思 / 日记」是手记分类，只出现在灵感手记；待办侧栏、过滤条、批量栏和新建任务标签不会列出它们。若某条待办或习惯已经打上，抽屉里仍显示以便去掉。
