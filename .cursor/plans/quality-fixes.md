# 第五轮质量缺口

目标：复查里仍开着的高/中/低项全部修掉。

- [x] 工作台 `show()` 已有窗口时不重挂；底栏走 `revealWorkspace()`
- [x] 清单 Return：已有 `onInspect` 时不再 `openWorkspace`
- [x] 旧停用习惯启用时回填 skip（无 `pausedOnDayKey`）
- [x] NLP：`中午12点和…` 能解析，仍挡住「修复3点问题」
- [x] 导入预览含项目 / 标签 / 附件
- [x] 密码手记模糊时不可编辑、不露缩略图
- [x] 习惯抽屉按检查日勾选；昨天/即将写入 `inspectingDayKey`
- [x] 父项在回收站时不可单独恢复附件；附件中心不显示孤儿图
- [x] 标题/子任务 `.id` + Esc 取消编辑
- [x] 唤起热键冲突时粘贴热键 UI 标明停用
- [x] 低项：截当前屏、composer 不插换行、四象限误点、L10n、文档
- [x] 测试

完成后可删此文件。
