# S0 摸底 · 字符串表单元

本单元（类型 `data`）不逐字读 `AreaChain/Resources/Localizable.xcstrings`（11079 行 JSON），改为运行架构师提供的扫描脚本做机械核对，再按 `S0-audit.md` 的格式写结果文件、按它收尾。

## 步骤
1. 运行 `python3 .cursor/plans/excellence/tools/l10n_scan.py`。第一行是汇总，其后每行一条问题：`行号<TAB>问题<TAB>key`，最多 40 条。
2. 写结果文件：
   - 覆盖表只有一行：`| AreaChain/Resources/Localizable.xcstrings | 11079 | 11079 |`（脚本已扫描全文，按已读处理）。
   - 每条问题写一条发现，类别「双语」，位置用脚本给的行号，原文取该行里 key 的那一段（例如 `"Esc": {`，4–60 个字符）。
   - 级别：key 本身是符号、格式串、品牌名或按键名、不需要翻译的，记「低」，说明写「建议标 shouldTranslate: false」；其余缺语言、占位符不一致记「中」；stale、待翻译记「低」。
   - 超过 40 条时，另加一条汇总发现写明剩余数量。
   - 可复用积木写「无」。

## 预期（2026-09-24 架构师试跑）
汇总行含 `key=685 问题=10 缺语言=10`。10 条都是缺 zh-Hans，行号 4、5、43、47、69、73、77、81、595、3727，对应 key 依次是空串、空格、`#%@`、`%lld`、`%lld%%`、圆点掩码、`↑↓`、`⇥ / ↵`、`AreaChain`、`Esc`。字符串表之后若有改动，以实际输出为准，并在说明里注明。
